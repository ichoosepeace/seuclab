#include <stdio.h>

int main(void) {
    /*char name[30];
    printf("What's your name?\n");
    scanf("%s", &name);
    printf("Welcome %s\n", name);
    if ( sizeof(name) < 6 ) {
        printf("Btw, Your name is too short, %s", name);
    } else {
        printf("Your name has %lu characters.", sizeof(name));
    } */

    int a;
    float b;
    char c;
    return 0;
}
